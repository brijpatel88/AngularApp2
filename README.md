# Angular Task Tracker

This project is a simple task tracker application built with Angular.

Features:
- Add tasks
- Mark tasks as completed
- Clear all tasks
- Custom Pipe (truncate text)
- Custom Directive (style completed tasks)
- TaskService using RxJS Observables

Built using:
- Angular
- RxJS
- TypeScript